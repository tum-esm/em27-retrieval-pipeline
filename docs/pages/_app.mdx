import '../style.css';
import { Crimson_Pro } from 'next/font/google';

export const crimsonProFont = Crimson_Pro({
    subsets: ['latin'],
});

export default function App({ Component, pageProps }) {
    return (
        <>
            <style jsx global>{`
                :root {
                    --next-font-google-crimson-pro: ${crimsonProFont.style
                        .fontFamily};
                }
            `}</style>
            <Component {...pageProps} />
        </>
    );
}
