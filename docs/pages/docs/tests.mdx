Order of the tests:

1. Static types (`quick`)
2. Config Template (`quick`) & Config (`integration`)
3. Utility functions (`quick`) & Metadata/Profiles connection (`integration`)
4. Mock Retrieval (`ci`)
5. Full Retrieval (`complete`)
